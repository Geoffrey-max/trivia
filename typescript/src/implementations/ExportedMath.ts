export class ExportedMath {
    
    floor(x: number) {
        return Math.floor(x);
    }

    round(x: number) {
        return Math.round(x);
    }

    random() {
        return Math.random();
    }
}